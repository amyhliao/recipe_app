<div class='ques-answer cf'>
  <h3>All Categories:</h3>
  <ul>
      <% @categories.each do |category| %>
          <li class="btn"><%= link_to category.name, category_path(category) %></li>
      <% end %>
  </ul>
</div>